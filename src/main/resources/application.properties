

mybatis.type-aliases-package=com.jiuli.liping.vo
spring.datasource.url = jdbc:sqlserver://192.168.5.78;Databasename=PresentDB
spring.datasource.username = sa
spring.datasource.password = JIULIfine2019
spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver

server.port = 8066
mybatis.mapper-locations = classpath:mapper/*Mapper.xml

#启用文件上传，默认为true
spring.servlet.multipart.enabled=true
#文件大于该阈值时，将写入磁盘（KB/MB）
spring.servlet.multipart.file-size-threshold=0KB
#单个文件最大值（KB/MB）
spring.servlet.multipart.max-file-size=10MB
#单个请求多个文件的最大值（KB/MB）
spring.servlet.multipart.max-request-size=10MB

file_hostpath=D://jiuliliping

#配置tymeleaf
spring.thymeleaf.model=HTML
spring.thymeleaf.encoding=UTF-8
spring.thymeleaf.cache=false

multipart.enabled= true

#将文件写入磁盘后的阈值。 值可以使用后缀“MB”或“KB”表示兆字节或千字节大小。
multipart.file-size-threshold= 0

#最大文件大小。 值可以使用后缀“MB”或“KB”表示兆字节或千字节大小。
multipart.max-file-size= 10Mb

#最大请求大小。 值可以使用后缀“MB”或“KB”表示兆字节或千字节大小。
multipart.max-request-size= 20Mb